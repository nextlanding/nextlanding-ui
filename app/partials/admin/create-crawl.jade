#create-crawl-section(ng-controller='AdminCreateCrawlCtrl').container.rounded-light-container
  fieldset
    legend Search Crawler
    .row
      .col-md-12
        form(name='searchCrawlerForm', novalidate).form-horizontal
          .form-group
            label.col-md-3.control-label(for='crawl-urls') crawl-urls
            .col-md-9
              textarea#crawl-urls.form-control(name='crawl-urls', required, ng-model="model.crawl_urls")
              p#help-block Each url goes on its own line
          .form-group
            .col-md-9.col-md-offset-3
              button.btn.btn-primary(ng-click='settings.crawlUrls()',type='button',ng-disabled='searchCrawlerForm.$invalid || settings.crawlerPending') Crawl
